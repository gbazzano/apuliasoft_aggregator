export class Employee {
    id: number;
    name: string;

    constructor(id: number, name: string) {
        // Validations here if required.

        this.id = id;
        this.name = name;
    }
}